/**
 * Application configuration.
 *
 * This project currently has no environment-based configuration to
 * centralize here: there is no backend API base URL (all data comes
 * from mock services under `services/`), no feature flags, and no
 * build-time environment branching beyond what Vite already provides
 * via `import.meta.env`.
 *
 * This file is scaffolded per the approved architecture so that when
 * real configuration is needed (e.g. an API base URL once services/
 * are wired to a live backend), it has an established home rather
 * than being added ad hoc inside individual modules.
 */
export {}
